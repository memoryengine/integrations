{
  "name": "@memoryengine/git-history",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backfill and incrementally sync git commits into memory engine",
  "main": "git-history.ts",
  "scripts": {
    "build": "bun build --compile git-history.ts --outfile me-git-history"
  },
  "dependencies": {
    "@memoryengine/client": "^0.2.8"
  }
}
